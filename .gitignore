# The wrapper's own two scripts and its jar are GENERATED (see gradle/wrapper/gradle-wrapper.properties),
# so they are ignored rather than committed: a wrapper half in the repository is the failure mode that
# file exists to avoid.
gradlew
gradlew.bat
gradle/wrapper/gradle-wrapper.jar

.gradle/
build/
*/build/
*/*/build/
local.properties
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/
.kotlin/
