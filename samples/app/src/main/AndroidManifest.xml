<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sample">

        <!--
          THE REDIRECT IS A CUSTOM SCHEME AND THE STRING MUST MATCH EXACTLY the one registered on the
          app row and the one passed to `SponsoredTokens`. The worker compares it as a WHOLE STRING
          and never as a prefix, which is what stops a redirect being pointed at somebody else's path.

          `singleTask` so the browser comes back into the activity that is already there rather than
          stacking a second copy of it; the redirect then arrives at `onNewIntent`.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.sponsoredtokens.sample" android:path="/oauth2redirect" />
            </intent-filter>
        </activity>
    </application>
</manifest>
