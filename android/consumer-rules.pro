# The library reflects over nothing, generates no code and names no class by string, so an app's
# shrinker has nothing to keep for it. This file exists so that adding a rule later is an edit
# rather than a new file every consumer has to learn about.
